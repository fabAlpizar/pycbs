Config file example
===================

This section shows example configuration blocks for pyCBS-style inputs.
Each code block is provided verbatim (INI-style) and grouped into logical
subsections: extrapolation schemes (HF / correlation / HF+correlation)
and geometrical optimization (gradient-based and SQM).

